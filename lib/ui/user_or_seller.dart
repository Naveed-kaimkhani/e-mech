import 'package:flutter/material.dart';

class User_or_Seller extends StatelessWidget {
  const User_or_Seller({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}